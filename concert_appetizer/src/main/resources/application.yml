spring:
  application:
    name: sidekick

  # 1. H2 Database ?? (??? ??)
  datasource:
    url: jdbc:h2:mem:sidekickdb;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  # 2. H2 Console ?? (??? DB ???)
  h2:
    console:
      enabled: true
      path: /h2-console  # http://localhost:8080/h2-console ? ??

  # 3. JPA ??
  jpa:
    hibernate:
      ddl-auto: update  # ?? ? ??? DB ??? ?? ?? (create-drop? ?? ???)
    properties:
      hibernate:
        format_sql: true        # SQL ??? ??
        show_sql: true          # ???? SQL ??? ??
    defer-datasource-initialization: true # data.sql ??? ?? ??

  # 4. API ?? (JSON ?? null ?? ??? ?)
  jackson:
    property-naming-strategy: SNAKE_CASE # (??) JSON ??? snake_case? ???? ?? ?

# 5. ?? ?? ??
server:
  port: 8080
